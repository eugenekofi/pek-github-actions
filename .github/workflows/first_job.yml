name: First Workflow

on:
  - push

jobs: 
    first_job: 
        name: jomacs-k8s
        runs-on: ubuntu-latest
        steps:
            - name: First Message
              run: echo "Welcome to my world of CICD through GitHub Actions"

            - name: List all Files including hidden
              run: ls -1a

            - name: Show Present Working Directory 
              run: pwd
